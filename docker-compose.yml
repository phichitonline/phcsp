version: '3.8'

services:
  # PHP Application
  app:
    # หากต้องการ Build เองที่เครื่อง:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    # หากต้องการดึงจาก Docker Hub (ใช้ชื่อ image ที่คุณตั้ง):
    image: ${DOCKER_USERNAME:-yourusername}/phcsp-app:${IMAGE_TAG:-latest}
    container_name: phcsp-app
    restart: unless-stopped
    working_dir: /var/www
    env_file:
      - .env
    volumes:
      - ./.env:/var/www/.env
      - ./storage:/var/www/storage
      - ./public/storage:/var/www/public/storage
      - public-build:/var/www/public
    networks:
      - phcsp-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: phcsp-nginx
    restart: unless-stopped
    ports:
      - "8888:80"
    volumes:
      - public-build:/var/www/public:ro
      - ./storage/app/public:/var/www/public/storage:ro
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    depends_on:
      - app
    networks:
      - phcsp-network

  # MySQL Database
  db:
    image: mysql:8.0
    container_name: phcsp-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
    ports:
      - "33060:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - phcsp-network

networks:
  phcsp-network:
    driver: bridge

volumes:
  dbdata:
    driver: local
  public-build:
